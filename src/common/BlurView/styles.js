import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  blurContainer: {
    position: 'absolute',
    width: '100%',
    height: '100%',
    top: 0,
    bottom: 0,
    left: 0,
    right: 0,
    opacity: 1,
  },
});

export default styles;
